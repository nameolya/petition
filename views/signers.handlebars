<h1>We are making the world a better place!</h1>
<h2>
    {{#each signers}}
    <ul>  {{#if url}}
        <a href={{url}}>{{this.fullname}}</a>  {{this.age}}  {{this.city}}
{{else}}
       {{this.fullname}}  {{this.age}}  {{this.city}}
         {{/if}}
    </ul>
    {{/each}}
    </h2>